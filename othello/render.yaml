services:
  - type: web
    name: othello
    env: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/othello-1.0.0.jar
    envVars:
      - key: PORT
        value: 8080
